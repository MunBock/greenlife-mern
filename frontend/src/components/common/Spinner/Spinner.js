import React from "react";

import "./spinner.css";

const Spinner = () => {
  return (
    <div className="spinner-place">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
